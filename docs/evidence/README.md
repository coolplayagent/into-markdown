# Compatibility evidence

Files in this directory are reproducible measurements or immutable corpus authorities. They are
not product configuration and do not change conversion outcomes.

The issue #270 DOCX corpus manifest is generated and verified with
`tools/docx-corpus-evidence.py`. Its package classification is computed before any converter is
invoked; observations may not rewrite that classification.
