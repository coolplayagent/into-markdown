# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "whisper-rs"
version = "0.16.0"
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Rust bindings for whisper.cpp"
documentation = "https://docs.rs/whisper-rs"
readme = "README.md"
license.workspace = true
repository = "https://codeberg.org/tazz4843/whisper-rs"

[features]
_gpu = []
coreml = ["whisper-rs-sys/coreml"]
cuda = [
    "whisper-rs-sys/cuda",
    "_gpu",
]
default = []
nightly-tests = []
hipblas = [
    "whisper-rs-sys/hipblas",
    "_gpu",
]
intel-sycl = [
    "whisper-rs-sys/intel-sycl",
    "_gpu",
]
log_backend = []
metal = [
    "whisper-rs-sys/metal",
    "_gpu",
]
openblas = ["whisper-rs-sys/openblas"]
openmp = ["whisper-rs-sys/openmp"]
raw-api = []
test-with-tiny-model = []
tracing_backend = []
vulkan = [
    "whisper-rs-sys/vulkan",
    "_gpu",
    "dep:libc",
]

[lib]
name = "whisper_rs"
path = "src/lib.rs"

[dependencies.libc]
version = "0.2"
optional = true

[dependencies.whisper-rs-sys]
version = "0.15"
